<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.app">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>
    <!-- 设置Wi-Fi -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE"/>
    <!-- 获取使用Wi-Fi等WLAN无线网络 -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <!-- GPS -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.READ_CONTACTS"/>
    <uses-permission android:name="android.permission.WRITE_CONTACTS"/>
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>

    <application
        android:name=".BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".activity.BitmapActivity"/>
        <activity android:name=".activity.DownloadActivity"/>
        <activity android:name=".activity.AndroidUtilsActivity"/>
        <activity android:name=".activity.PermissionsActivity"/>
        <activity android:name=".activity.LoginActivity"/>
        <activity android:name=".activity.VoiceActivity"/>
        <activity android:name=".activity.ListView01Activity"/>
        <activity android:name=".activity.FreshActivity"/>
        <activity android:name=".activity.dialog.DialogActivity"/>
        <activity
            android:name=".activity.ExpListViewActivity"
            android:label="@string/title_activity_exp_list_view"/>
        <activity android:name=".activity.retrofit.RetrofitActivity">
        </activity>
        <activity android:name=".activity.retrofit.RetrofitHttpActivity">
        </activity>
        <activity android:name=".activity.retrofit.FileActivity">
        </activity>
        <activity android:name=".activity.ButtonProgressActivity">
        </activity>
        <activity android:name=".activity.RecyclerActivity">
        </activity>
        <activity android:name=".activity.MediaStoreActivity">
        </activity>
        <activity android:name=".activity.MkActivity">
        </activity>
    </application>

</manifest>